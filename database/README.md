# Database

SQLite migrations and database operations will be added in the backend foundation phase. Development databases must remain untracked; see the root `.gitignore`.

The configured development path is `DATABASE_PATH` from `.env`, defaulting to `./database/taptalk.db` when the database layer is implemented.